diff --git a/src/game/backend/PersonalVehicles.cpp b/src/game/backend/PersonalVehicles.cpp
index ecfe2f5..0c1b741 100644
--- a/src/game/backend/PersonalVehicles.cpp
+++ b/src/game/backend/PersonalVehicles.cpp
@@ -259,7 +259,9 @@ namespace YimMenu
 	{
 		m_Model = m_Data->VehicleModel;
 		m_Plate = m_Data->NumberPlateText;
-		m_Name = std::format("{} ({})##{}", HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(VEHICLE::GET_DISPLAY_NAME_FROM_VEHICLE_MODEL(m_Model)), m_Plate, m_Id);
+		// Append the language-independent GXT key so the list stays searchable with a US keyboard layout.
+		const char* gxtKey = VEHICLE::GET_DISPLAY_NAME_FROM_VEHICLE_MODEL(m_Model);
+		m_Name = std::format("{} [{}] ({})##{}", HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(gxtKey), gxtKey, m_Plate, m_Id);
 
 		SetGarage();
 	}
diff --git a/src/game/frontend/submenus/Self/Weapons.cpp b/src/game/frontend/submenus/Self/Weapons.cpp
index 3040f94..539703e 100644
--- a/src/game/frontend/submenus/Self/Weapons.cpp
+++ b/src/game/frontend/submenus/Self/Weapons.cpp
@@ -82,7 +82,12 @@ namespace YimMenu::Submenus
 							std::string nameDisplay = HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(nameGxt.c_str());
 							std::string descDisplay = HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(descGxt.c_str());
 
-							weaponDisplays.push_back({((nameDisplay.empty() || nameDisplay == "NULL" || nameDisplay == "Invalid") ? "" : nameDisplay), ((descDisplay.empty() || descDisplay == "NULL" || descDisplay == "Invalid") ? "" : descDisplay), weap});
+							std::string name = (nameDisplay.empty() || nameDisplay == "NULL" || nameDisplay == "Invalid") ? "" : nameDisplay;
+							// Append the language-independent GXT label so the row stays searchable with a US keyboard layout.
+							if (!name.empty() && !nameGxt.empty() && nameGxt != "NULL")
+								name += "  [" + nameGxt + "]";
+
+							weaponDisplays.push_back({name, ((descDisplay.empty() || descDisplay == "NULL" || descDisplay == "Invalid") ? "" : descDisplay), weap});
 						}
 
 						thread->Kill();
diff --git a/src/game/frontend/submenus/Vehicle/SpawnVehicle.cpp b/src/game/frontend/submenus/Vehicle/SpawnVehicle.cpp
index 32a2f47..c4e7dc7 100644
--- a/src/game/frontend/submenus/Vehicle/SpawnVehicle.cpp
+++ b/src/game/frontend/submenus/Vehicle/SpawnVehicle.cpp
@@ -44,12 +44,20 @@ namespace YimMenu::Submenus
 						}
 						++count;
 
-						std::string maker = HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(VEHICLE::GET_MAKE_NAME_FROM_VEHICLE_MODEL(veh));
+						std::string makerKey = VEHICLE::GET_MAKE_NAME_FROM_VEHICLE_MODEL(veh); // language-independent ASCII key
+						std::string maker    = HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(makerKey.c_str());
 						if (maker != "NULL")
 						{
 							finalName = maker + " " + finalName;
 						}
 
+						// Append the language-independent GXT key(s) in brackets so the row stays
+						// searchable with a US keyboard layout even when the display name is localized.
+						std::string searchKey = gxt;
+						if (!makerKey.empty() && makerKey != "NULL")
+							searchKey = makerKey + " " + gxt;
+						finalName += "  [" + searchKey + "]";
+
 						vehicleNames.push_back(finalName);
 
 						int id = VEHICLE::GET_VEHICLE_CLASS_FROM_NAME(veh);
@@ -85,7 +93,15 @@ namespace YimMenu::Submenus
 
 			const int visible = std::min(20, static_cast<int>(vehicleNames.size()));
 			const float height = visible * ImGui::GetTextLineHeightWithSpacing();
-			if (ImGui::BeginListBox("##vehicles", {300.f, height}))
+			// Size the list to the widest entry (min 300) so appended GXT keys don't get clipped.
+			static float listWidth = 0.f;
+			if (listWidth == 0.f && !vehicleNames.empty())
+			{
+				for (const auto& n : vehicleNames)
+					listWidth = std::max(listWidth, ImGui::CalcTextSize(n.c_str(), nullptr, true).x);
+				listWidth += ImGui::GetStyle().FramePadding.x * 2 + ImGui::GetStyle().ScrollbarSize + 8.f;
+			}
+			if (ImGui::BeginListBox("##vehicles", {std::max(listWidth, 300.f), height}))
 			{
 				if (vehicleNames.empty())
 				{
@@ -175,7 +191,11 @@ namespace YimMenu::Submenus
 
 			const int visible = std::min(20, static_cast<int>(PersonalVehicles::GetPersonalVehicles().size()));
 			const float height = visible * ImGui::GetTextLineHeightWithSpacing();
-			if (ImGui::BeginListBox("##personalvehicles", {300.f, height}))
+			// Size the list to the widest entry (min 300) so appended GXT keys don't get clipped.
+			float pvWidth = 300.f;
+			for (const auto& it : PersonalVehicles::GetPersonalVehicles())
+				pvWidth = std::max(pvWidth, ImGui::CalcTextSize(it.first.c_str(), nullptr, true).x + ImGui::GetStyle().FramePadding.x * 2 + ImGui::GetStyle().ScrollbarSize + 8.f);
+			if (ImGui::BeginListBox("##personalvehicles", {pvWidth, height}))
 			{
 				if (PersonalVehicles::GetPersonalVehicles().empty())
 				{
diff --git a/src/game/frontend/submenus/World/SpawnPed.cpp b/src/game/frontend/submenus/World/SpawnPed.cpp
index c291621..afbdb4b 100644
--- a/src/game/frontend/submenus/World/SpawnPed.cpp
+++ b/src/game/frontend/submenus/World/SpawnPed.cpp
@@ -35,7 +35,15 @@ namespace YimMenu::Submenus
 
 			const int visible = std::min(20, static_cast<int>(g_PedModels.size()));
 			const float height = visible * ImGui::GetTextLineHeightWithSpacing();
-			if (ImGui::BeginListBox("##peds", {300.f, height}))
+			// Size the list to the widest entry (min 300), consistent with the other spawn lists.
+			static float pedWidth = 0.f;
+			if (pedWidth == 0.f && g_PedModels.size() != 0)
+			{
+				for (size_t i = 0; i < g_PedModels.size(); i++)
+					pedWidth = std::max(pedWidth, ImGui::CalcTextSize(g_PedModels[i], nullptr, true).x);
+				pedWidth += ImGui::GetStyle().FramePadding.x * 2 + ImGui::GetStyle().ScrollbarSize + 8.f;
+			}
+			if (ImGui::BeginListBox("##peds", {std::max(pedWidth, 300.f), height}))
 			{
 				std::string lower = search;
 				std::transform(lower.begin(), lower.end(), lower.begin(), ::tolower);
